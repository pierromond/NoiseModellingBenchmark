this gradle project was created only to make editing the script fast.

You have to run this command at least on time to download the dependencies before opening settings.gradle in IntelliJ IDEA the first time:

```bash
bash benchmark_run.sh
```

